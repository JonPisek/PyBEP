#!/usr/bin/env python

from setuptools import setup

# an almost empty file for now, use this for help:
# https://ianhopkinson.org.uk/2022/02/understanding-setup-py-setup-cfg-and-pyproject-toml-in-python/


# this line checks if the setup.py script is run as the main script (and is not called by some
# other script); if that is the case, the if clause evaluates to True and setup() is called.
if __name__ == "__main__":

    setup()